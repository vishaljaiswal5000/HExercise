export enum Collection {
  FAVORITES = 'favorites',
  USER = 'users',
}

export enum AppRoutes {
  DASHBOARD = 'dashboard',
  SIGNIN = 'signin',
  SIGNUP = 'signup',
}

export enum Urls {
  HELIUM_EXERCISE_BODY_PARTS = 'bodyPartList',
  HELIUM_EXERCISE_BODY_EXERCISE = 'bodyPart',
}
